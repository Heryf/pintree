# 2026-08-01

## 修复首页合集展示 & 改为树形文件夹布局

- 修复前台首页仅展示默认第一个合集的问题：改为无 `collection` 参数时展示所有公开合集卡片网格。
- 新增 `src/components/collection/CollectionGrid.tsx`：首页合集选择入口，显示合集名称、描述、公开状态、书签数量。
- 改造 `src/app/page.tsx`：无合集参数时渲染 `CollectionGrid`；有参数时渲染侧边栏 + 书签网格。
- 改造 `src/components/website/sidebar.tsx`：顶部增加「返回合集列表」与合集下拉切换器；使用父组件传入的合集列表。
- 改造 `src/components/bookmark/BookmarkGrid.tsx`：改为导航式布局——上方展示当前文件夹的直接子文件夹（绿色大卡片），下方展示当前文件夹的书签，不再展开深层内容。
- 改造 `src/components/bookmark/FolderCard.tsx`：绿色渐变方形卡片，居中显示文件夹图标、名称与书签数量。
- 本地 `next build` 类型检查通过；因未配置 DATABASE_URL，静态生成阶段数据库报错（部署时配置环境变量即可）。
